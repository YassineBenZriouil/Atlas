"""First-party optional integrations, each shaped as an atlas.plugins.AtlasPlugin.

Unlike third-party plugins in the top-level plugins/ directory, these ship
with ATLAS itself but stay isolated from core (Atlas.md section 58): core
must never import from here."""
